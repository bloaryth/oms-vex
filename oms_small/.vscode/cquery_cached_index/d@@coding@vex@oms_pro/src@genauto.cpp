#include "main.h"
#include <thread>
#include <vector>

struct Record {
  std::vector<double> motor;
};

void autGen(std::vector<pros::Motor&> MotorVector) {
  // for iter : MotorVector
  // MotorVector
  // E_CONT

  // E_CONTROLLER_ANALOG_LEFT_Y
}
